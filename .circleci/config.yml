version: 2.1

jobs:
  
  build-medium-amd64:
    machine:
      image: ubuntu-2004:202101-01
   
    working_directory: ~/seata-samples

    steps:
      
      
      - checkout
      - run:
          name: ️️🏗️ pip dev requirements
          command: |
           docker run --name seata-server -p 8091:8091 -e SEATA_IP=192.168.1.1 -e SEATA_PORT=8091 seataio/seata-server
